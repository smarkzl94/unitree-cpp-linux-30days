# 2026-08-24 · Day 07 · Week1 复盘 + 冒烟脚本

## 今日目标
- 不写新功能，把 Day01–06 收成能口述的一条线
- Linux：`week1_smoke.sh` 一键编跑 Day01 + Day06

## 核心知识点（3–7 条）
1. 本周主线：所有权 → 容器扩容/失效 → 堆+表调度 → 定长环形缓冲
2. 有裸资源才写五件套；否则 Rule of Zero
3. 移动：`std::move` / 临时量 / return / vector 扩容（移动需 `noexcept` 才优先走）
4. `emplace_back` 原地构造；`push_back` 常先有临时对象
5. 调度：权威在 map，堆可脏；RingBuffer 用 `size_` 区分空满，满则覆盖最旧
6. 冒烟脚本：`set -e`，先 `cd` 到仓库根，失败立刻停
7. 完整 Week1 清单见 `notes/recap/week1-recap.md`

## 代码要点 / API
- RingBuffer：`push` / `pop` / `size` / `full` / `empty`（覆盖最旧）
- 脚本：`week1-cpp-basics/day07-review/week1_smoke.sh`
- 总复盘：`notes/recap/week1-recap.md`

## 易错点 / 坑
- 冒烟相对路径相对的是启动时的 cwd，必须先 cd 到 ROOT
- Day06 只有 `main.cpp`（模板在头文件），不要 `g++ *.h`
- 本机无 `/proc`、无 ASan 时笔记写明「未采集 / 未跑」

## 面试口述（2 题）
1. Q：五件套什么时候必须自己写？  
   A：有成对申请/释放的裸资源时。成员已是 RAII 则不用写。
2. Q：RingBuffer 空和满怎么区分？满了怎么办？  
   A：另存 size（或牺牲一格）。遥测覆盖最旧；指令可拒绝。

## 明天预告
Day08：线程安全队列 + Linux 线程视角
