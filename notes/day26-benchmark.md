# Day26 · 压测记录（概念稿）

日期：2026-09-12  
状态：Hub 代码未写；先定口径。实跑后把数字填进「改前/改后」。

## 环境

- 机器 / WSL：待上机
- 编译：测速用 Release；正确性另用 Debug+ASan（两套分开报）
- 日志：`--log=warn`，关每帧 Debug
- `perf`：有则 10 秒，没有就写不可用

## 目标（设计，不是 bug）

- 产：100Hz
- 发：50Hz（入 100 出 50 是降采样，不叫丢包）

## 指标定义（上机沿用，不要改口径）

- **延迟**：`now_mono - frame.stamp_ns`（传感打戳 → 处理完或 client 收到）。只用单调时钟。
- **P50 / P99**：窗口内延迟排序后 50%、99% 位置。不用平均值冒充 P99。
- **丢包**：RingBuffer 覆盖次数（+ latest 未发送就被顶掉，若实现了）。TCP 不丢字节；这是应用层主动丢旧遥测。
- **频率**：每秒 `frames_in` / `frames_sent`。

## 改前（待测）

- P50=  P99=  drop=  CPU=  最热线程=

## 打算修的 1 点（上机时选「真测到的」）

对照清单，只改一处：

1. 每帧日志 / `endl` / `stringstream`
2. 忙等 `while (!pop)`
3. 持锁 `send`
4. 每帧 `vector`/`string` 分配
5. `sleep(10ms)` 不管已耗时，改 `sleep_until(next)`
6. Send-Q 堆但无覆盖 → 只发 latest（丢包升、P99 降是正确权衡）

改动：
现象 → 假设 → 改动 → 数字变化：

## 改后（待测）

- P50=  P99=  drop=  CPU=

## 结论

没数字先不编。没变也算完成：排除了一个假设。
