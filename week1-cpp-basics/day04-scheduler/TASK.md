# Day 04 · 任务优先级调度器（2026-08-21）

## 目标
用 `priority_queue` + `unordered_map` 实现任务调度。

## 必须实现
- 添加任务（id, priority, payload）
- 取出当前最高优先级任务
- 按 id 修改优先级（允许延迟删除策略）
- 按 id 查询是否存在

## 复盘
堆不能直接改内部元素时，常用什么技巧？
