# Day23 · Linux 笔记（ss vs 抓包，可选）

日期：2026-09-09  
环境：（WSL2 Ubuntu / 抓不到就写原因：______）

tcpdump 可选。搞不定就跳过，C++ 拆包才是今天重点。

---

## ss vs tcpdump（自己填）

- `ss` 告诉你：
- `tcpdump` 告诉你：
- 本机 127.0.0.1 互连，网卡该用 `eth0` 还是 `lo`：

## 可选：我试了没有

- 试过的命令 / 失败原因：
- 不行就改用 `strace` 看 send/recv（有没有）：

## 我踩过的坑

-
